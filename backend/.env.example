# MongoDB is optional for a local demo. Without it, the server starts an in-memory replica set.
MONGO_URI=mongodb://127.0.0.1:27017/operation-sentinel
JWT_SECRET=replace-this-with-a-long-random-secret
CLIENT_URL=http://localhost:5173
PORT=5000
